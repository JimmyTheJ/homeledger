@model HomeLedger.Web.Models.ImportUploadModel
@{
    ViewData["Title"] = "Import";
    var batches = ViewBag.Batches as IEnumerable<HomeLedger.Core.Entities.ImportBatch> ?? [];
}

<hgroup>
    <h1>Import</h1>
    <p>
        Upload a <strong>bank CSV</strong>, a <strong>PDF statement</strong> (requires LLM — see
        <a asp-controller="Settings" asp-action="Index">Settings</a>), or a
        <strong>ledger export</strong> from <a asp-controller="Export" asp-action="Index">Export</a>.
    </p>
</hgroup>

<form asp-action="Upload" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly"></div>

    <label>
        File
        <input asp-for="File" type="file" accept=".csv,.pdf,text/csv,application/pdf" required />
    </label>

    <div class="grid">
        <label>
            Account <small>(bank CSV &amp; PDF)</small>
            <select asp-for="AccountId" asp-items="ViewBag.Accounts">
                <option value="">—</option>
            </select>
        </label>
        <label>
            Entity <small>(bank CSV &amp; PDF)</small>
            <select asp-for="LedgerEntityId" asp-items="ViewBag.Entities">
                <option value="">—</option>
            </select>
        </label>
    </div>

    <label>
        <input asp-for="AutoAccept" type="checkbox" role="switch" />
        Auto-accept all suggestions (bank CSV &amp; PDF; skips review)
    </label>

    <button type="submit">Import</button>
</form>

@if (batches.Any())
{
    <section>
        <h2>Recent imports</h2>
        <table>
            <thead>
                <tr>
                    <th>File</th>
                    <th>Status</th>
                    <th>Date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var batch in batches)
                {
                    <tr>
                        <td>@batch.FileName</td>
                        <td>@batch.Status</td>
                        <td>@HomeLedgerFormats.FormatDate(DateOnly.FromDateTime(batch.CreatedAt.ToLocalTime())) @batch.CreatedAt.ToLocalTime().ToString("HH:mm")</td>
                        <td>
                            @if (batch.Status == HomeLedger.Core.Entities.ImportBatchStatus.Reviewing)
                            {
                                <a asp-action="Review" asp-route-id="@batch.Id">Continue review</a>
                            }
                            else
                            {
                                <a asp-action="Complete" asp-route-id="@batch.Id">View</a>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </section>
}
